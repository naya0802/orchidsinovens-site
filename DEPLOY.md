# Deploy to Vercel

Upload the entire `orchidsinovens-site` folder to Vercel Drop.

Do not upload only `index.html`. The site also needs:

- `styles.css`
- `assets/`
- `vercel.json`

If the site appears as blue links and black text, the stylesheet was not uploaded or is not being found.
